<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.8.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <style>
      .scrollbox {
        overflow: auto;
        scrollbar-width: thin; /* Firefox */
        scrollbar-color: rgb(129, 128, 128) transparent;
      }

      /* Chrome/Edge/Safari */
      .scrollbox::-webkit-scrollbar {
        width: 8px;
        height: 8px;
      }
      .scrollbox::-webkit-scrollbar-track {
        background: transparent;
      }
      .scrollbox::-webkit-scrollbar-thumb {
        background: rgba(0, 0, 0, 0.22);
        border-radius: 999px;
        border: 2px solid transparent;
        background-clip: content-box;
      }
      .scrollbox::-webkit-scrollbar-thumb:hover {
        background: rgba(0, 0, 0, 0.35);
        background-clip: content-box;
      }
    </style>
  </head>
  <body class="">
    <div class="main bg-zinc-800 w-full h-screen p-5 flex">
      <div class="sideBar w-[20%] h-full bg-zinc-600 rounded-lg"></div>
      <div class="mainSide w-full flex flex-col px-5 gap-5">
        <div class="navBar flex w-full m-auto p-5 rounded-lg text-zinc-200">
          <div class="firstHalf w-full">
            <h5>All Notes</h5>
          </div>
          <h5
            id="createBtn"
            class="bg-cyan-600 px-5 py-2 rounded-lg cursor-pointer hover:bg-cyan-700"
          >
            Create
          </h5>
        </div>
        <div
          class="notes relative w-full scrollbox h-full border-6 border-zinc-400 rounded-lg grid grid-cols-1 md:grid-cols-2 gap-5 overflow-y-auto p-5"
        >
          <% if(allNotes.length > 0 && allNotes ){ %> <%
          allNotes.forEach((note)=> { %>
          <div
            class="noteCard w-full h-56 bg-zinc-500 rounded-lg p-5 flex flex-col gap-3"
          >
            <div
              class="noteTitleDiv flex justify-between border-b-2 border-zinc-400 pb-2"
            >
              <h5 class="noteTitle text-zinc-950 font-semibold text-xl">
                <%= note.title %>
              </h5>
              <div class="btns">
                <i
                  class="editBtn border-2 border-zinc-600 px-3 py-2 rounded-lg text-zinc-200 cursor-pointer bg-green-600 hover:bg-green-700 ri-pencil-fill"
                ></i>
                <a href="dashboard/delete/<%= note._id %>">
                  <i
                    class="border-2 border-zinc-600 px-3 py-2 rounded-lg text-zinc-200 cursor-pointer bg-red-600 hover:bg-red-700 ri-delete-bin-5-line"
                  ></i
                ></a>
              </div>
            </div>
            <p class="text-zinc-900 overflow-y-auto"><%= note.description %></p>
          </div>
          <div class="editNoteForm hidden absolute top-2 right-2">
            <form
              action="/notes/dashboard/edit/<%= note._id %>"
              method="post"
              class="editForm flex flex-col gap-5 bg-indigo-500 shadow-xl shadow-black p-5 rounded-lg"
            >
              <input
                class="bg-zinc-200 outline-none rounded-lg px-3 py-1"
                type="text"
                placeholder="title"
                name="title"
              />
              <textarea
                class="bg-zinc-200 outline-none rounded-lg px-3 resize-none h-28 py-1"
                type="text"
                placeholder="description"
                name="description"
              ></textarea>
              <div class="btns flex gap-2 justify-between">
                <input
                  value="cancle"
                  type="button"
                  class="cancleBtn w-1/2 text-center bg-red-600 text-white px-3 py-1 cursor-pointer rounded-lg hover:bg-red-700"
                />
                <input
                  value="confirm"
                  type="submit"
                  class="confirmBtn w-1/2 text-center bg-green-600 text-white px-3 py-1 cursor-pointer rounded-lg hover:bg-green-700"
                />
              </div>
            </form>
          </div>
          <% }) %> <% } else{ %>
          <div class="noNotes">There is no notes created yet!</div>
          <% } %>
          <div id="newNoteForm" class="hidden absolute top-2 right-2">
            <form
              id="createForm"
              action="/notes/dashboard/create"
              method="post"
              class="createForm flex flex-col gap-5 bg-orange-500 shadow-xl shadow-black p-5 rounded-lg"
            >
              <input
                class="bg-zinc-200 outline-none rounded-lg px-3 py-1"
                type="text"
                placeholder="title"
                name="title"
              />
              <textarea
                class="bg-zinc-200 outline-none rounded-lg px-3 resize-none h-28 py-1"
                type="text"
                placeholder="description"
                name="description"
              ></textarea>
              <div class="btns flex gap-2 justify-between">
                <input
                  value="cancle"
                  type="button"
                  class="cancleBtn w-1/2 text-center bg-red-600 text-white px-3 py-1 cursor-pointer rounded-lg hover:bg-red-700"
                />
                <input
                  value="confirm"
                  type="submit"
                  class="confirmBtn w-1/2 text-center bg-green-600 text-white px-3 py-1 cursor-pointer rounded-lg hover:bg-green-700"
                />
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <script src="/scripts/script.js"></script>
  </body>
</html>
